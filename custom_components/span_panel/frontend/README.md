# Frontend Assets

Built JS bundles from the [span-card](https://github.com/SpanPanel/span-card)
repository (`integration-panel` branch).

To update: build span-card (`npm run build`) and copy `dist/` here.

This directory will be converted to a git submodule when the
`integration-panel` branch is pushed to the remote.

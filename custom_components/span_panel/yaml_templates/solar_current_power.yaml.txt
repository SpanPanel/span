  {{sensor_key}}:
    name: "Solar Current Power"
    entity_id: "{{entity_id}}"
    formula: "leg1_power + leg2_power"
    variables:
      leg1_power: "{{leg1_power_entity}}"
      leg2_power: "{{leg2_power_entity}}"
    attributes:
      tabs: "{{tabs_attribute}}"
      voltage: {{voltage_attribute}}
      amperage:
        formula: "(leg1_power + leg2_power) / {{voltage_attribute}}"
        metadata:
          unit_of_measurement: "A"
          device_class: "current"
          suggested_display_precision: {{power_display_precision}}
    metadata:
      unit_of_measurement: "W"
      device_class: "power"
      state_class: "measurement"
      suggested_display_precision: {{power_display_precision}}

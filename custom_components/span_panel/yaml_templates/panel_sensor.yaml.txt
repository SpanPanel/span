  {{sensor_key}}:
    name: "{{sensor_name}}"
    entity_id: "{{entity_id}}"
    formula: state
    alternate_states:
      FALLBACK:
        formula: 0.0
    attributes:
      voltage: 240
      amperage:
        formula: "state / 240"
        metadata:
          unit_of_measurement: "A"
          device_class: "current"
          suggested_display_precision: {{power_display_precision}}
    metadata:
      unit_of_measurement: "W"
      device_class: "power"
      state_class: "measurement"
      suggested_display_precision: {{power_display_precision}}
